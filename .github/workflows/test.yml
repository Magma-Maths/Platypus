name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies (Ubuntu)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y subversion
          # Install latest bats from source to avoid bats 1.10.0 bugs
          git clone --depth 1 https://github.com/bats-core/bats-core.git /tmp/bats
          sudo /tmp/bats/install.sh /usr/local
          bats --version

      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        run: brew install bats-core subversion

      - name: Set up Git identity
        run: |
          git config --global user.name "Test User"
          git config --global user.email "test@example.com"
          git config --global init.defaultBranch main

      - name: Run tests (excluding Docker tests)
        run: ./test/run-tests.sh --tests --no-docker

  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install shellcheck
        run: sudo apt-get install -y shellcheck

      - name: Run shellcheck
        run: ./test/run-tests.sh --shellcheck

  test-docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install bats-core and subversion
        run: |
          sudo apt-get update
          sudo apt-get install -y subversion
          # Install latest bats from source
          git clone --depth 1 https://github.com/bats-core/bats-core.git /tmp/bats
          sudo /tmp/bats/install.sh /usr/local
          bats --version

      - name: Set up Git identity
        run: |
          git config --global user.name "Test User"
          git config --global user.email "test@example.com"
          git config --global init.defaultBranch main

      - name: Start SVN server
        run: |
          docker compose -f test/docker/svn-server/docker-compose.yml up -d --wait
          # Wait for SVN to be ready
          for i in {1..30}; do
            docker exec platypus-svn-server svnadmin info /home/svn/repos && break
            sleep 1
          done

      - name: Run Docker tests
        run: ./test/run-tests.sh --docker

      - name: Stop SVN server
        if: always()
        run: docker compose -f test/docker/svn-server/docker-compose.yml down -v

